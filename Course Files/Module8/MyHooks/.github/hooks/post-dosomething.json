{
	"hooks": {
		"PostToolUse": [
			{
				"type": "command",
				 "command": "powershell -NoProfile -Command \"$inputJson = [Console]::In.ReadToEnd(); $ts = (Get-Date).ToString('o'); Add-Content -Path .copilot-hook.log -Value ('[' + $ts + '] PostToolUse fired');\""
			}
		]
	}
}