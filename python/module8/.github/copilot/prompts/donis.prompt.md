---
mode:'agent'                 #---
mode: 'agent'                 # ask | edit | agent
model: GPT-4o                 # or omit to use current picker model
description: Optimize code for runtime efficiency
---

Create a Fibonacci function. Then:
- reduce allocations
- avoid unnecessary abstractions
- prefer iterators/streaming if possible
Show the final code and a short rationale.: GPT-4o                 # or omit to use current picker model
description: Optimize code for runtime efficiency
---

Create a Fibonacci function. Then:
- reduce allocations
- avoid unnecessary abstractions
- prefer iterators/streaming if possible
Show the final code and a short rationale.